import { InsufficientData } from '../InsufficientData.js';

export function ForecastCone() {
  return (
    <InsufficientData
      eyebrow="2026 price forecast"
      caption="Price forecasts (shown as ranges, not single numbers) will be added once Bright MLS / NAR forecast data is ingested."
    />
  );
}
